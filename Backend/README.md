# User Management Dashboard API (Express + PostgreSQL + Sequelize)

REST API for managing users with nested address/geo, validation, search/sort/pagination, and robust JSON error handling.

## Prerequisites
- Node.js 18+ recommended
- PostgreSQL 13+

## Setup

1) Clone / copy the project
2) Install dependencies:
